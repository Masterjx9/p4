[package]
name = "p4-ffi"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "C ABI bindings for p4-core"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
p4-core = { path = "../p4-core" }
serde_json = "1"


