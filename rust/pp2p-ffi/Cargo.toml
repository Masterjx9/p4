[package]
name = "pp2p-ffi"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "C ABI bindings for pp2p-core"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
pp2p-core = { path = "../pp2p-core" }
serde_json = "1"

